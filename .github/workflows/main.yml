name: Label Actions

on:
  discussion:
    types: [created, unlabeled]

permissions:
  discussions: write

jobs:
  label:
    name: Label Actions
    runs-on: ubuntu-latest
    steps:
      - name: Label Actions
        uses: dessant/label-actions@v4.0.1
        with:
          github-token: ${{ secrets.TOKEN }}
