name: Label Actions

on:
  discussion:
    types: [created, unlabeled]

permissions:
  discussions: write

jobs:
  label:
    name: Label Actions
    runs-on: ubuntu-latest
    steps:
      - name: Label Actions
        uses: dessant/label-actions@v4
        with:
          github-token: ${{ secrets.token }}
